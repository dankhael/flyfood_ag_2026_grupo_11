[pytest]
testpaths = tests

# Mostra os logs INFO inline durante a execucao (logs didaticos),
# sem o "flood" do GA: os testes rodam o algoritmo com verbose=False
# e emitem apenas algumas linhas-resumo via logging.
log_cli = true
log_cli_level = INFO
log_cli_format = %(message)s

addopts = -v
